name: Apply Japanese deck and stats update

on:
  push:
    branches:
      - agent/japanese-deck-import-export-stats

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/japanese-deck-import-export-stats
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply reviewed implementation
        shell: python
        run: |
          from pathlib import Path
          import re

          def replace_one(path, old, new):
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f'target not found in {path}: {old[:100]!r}')
              p.write_text(text.replace(old, new, 1))

          def replace_regex(path, pattern, replacement):
              p = Path(path)
              text = p.read_text()
              next_text, count = re.subn(pattern, replacement, text, count=1, flags=re.S)
              if count != 1:
                  raise SystemExit(f'pattern count {count} in {path}: {pattern[:100]!r}')
              p.write_text(next_text)

          Path('src/anki/localization.ts').write_text(r'''import type { NoteTypeDefinition } from '../types.js';
import {
  BASIC_NOTE_TYPE_ID,
  BASIC_OPTIONAL_REVERSE_NOTE_TYPE_ID,
  BASIC_REVERSE_NOTE_TYPE_ID,
  CLOZE_NOTE_TYPE_ID,
  IMAGE_OCCLUSION_NOTE_TYPE_ID,
  TYPE_ANSWER_NOTE_TYPE_ID
} from './defaults.js';

const BUILTIN_NOTE_TYPE_NAMES: Record<string, string> = {
  [BASIC_NOTE_TYPE_ID]: '基本',
  [BASIC_REVERSE_NOTE_TYPE_ID]: '基本（表裏2枚）',
  [BASIC_OPTIONAL_REVERSE_NOTE_TYPE_ID]: '基本（任意で表裏2枚）',
  [TYPE_ANSWER_NOTE_TYPE_ID]: '基本（解答入力）',
  [CLOZE_NOTE_TYPE_ID]: '穴埋め',
  [IMAGE_OCCLUSION_NOTE_TYPE_ID]: '画像穴埋め'
};

const STOCK_NAME_TRANSLATIONS: Record<string, string> = {
  'Basic': '基本',
  'Basic (and reversed card)': '基本（表裏2枚）',
  'Basic (optional reversed card)': '基本（任意で表裏2枚）',
  'Basic (type in the answer)': '基本（解答入力）',
  'Cloze': '穴埋め',
  'Image Occlusion': '画像穴埋め'
};

const FIELD_TRANSLATIONS: Record<string, string> = {
  Front: '表面',
  Back: '裏面',
  Extra: '補足',
  'Add Reverse': '裏面カードを追加',
  Text: '文章',
  'Back Extra': '裏面の補足',
  Occlusions: 'マスク情報',
  Masks: 'マスク情報',
  Image: '画像',
  Header: '見出し',
  Comments: 'コメント'
};

export function noteTypeDisplayName(type: Pick<NoteTypeDefinition, 'id' | 'name'>): string {
  return BUILTIN_NOTE_TYPE_NAMES[type.id] ?? STOCK_NAME_TRANSLATIONS[type.name] ?? type.name;
}

export function noteFieldDisplayName(name: string): string {
  return FIELD_TRANSLATIONS[name] ?? name;
}

export function noteTypeKindDisplayName(kind: NoteTypeDefinition['kind']): string {
  if (kind === 'cloze') return '穴埋め';
  if (kind === 'image-occlusion') return '画像穴埋め';
  return '標準';
}
''')

          # app.ts imports, state and user-facing labels
          replace_one('src/app/app.ts',
              "import type { ReviewMode, ReviewSession, ReviewStyle, StudyCard } from '../types.js';",
              "import type { ReviewMode, ReviewSession, ReviewStyle, StudyCard } from '../types.js';\nimport { exportAnkiPackage, importAnkiPackage } from '../anki/anki-package.js';")
          replace_one('src/app/app.ts',
              "import { DEFAULT_DECK_ID } from '../anki/defaults.js';",
              "import { DEFAULT_DECK_ID } from '../anki/defaults.js';\nimport { noteFieldDisplayName, noteTypeDisplayName } from '../anki/localization.js';")
          replace_one('src/app/app.ts',
              "import { dailyCounts, summarize, tagAccuracy } from '../statistics/stats.js';",
              "import { dailyCounts } from '../statistics/stats.js';")
          replace_one('src/app/app.ts',
              "  private statusTimer = 0;",
              "  private statusTimer = 0;\n  private statsRange: 'week' | 'month' | 'year' | 'all' = 'week';")
          replace_one('src/app/app.ts',
              "const importButton = button('ファイルを読み込む', 'anki-button');",
              "const importButton = button('Ankiデッキを読み込む', 'anki-button');")
          replace_one('src/app/app.ts',
              "    const custom = button('カスタム学習', 'anki-button'); custom.addEventListener('click', () => void this.openStudyDialog(undefined, deck.id));\n    const back = button('デッキ一覧', 'anki-button'); back.addEventListener('click', () => { this.selectedDeckId = null; void this.render(); });\n    actions.append(start, custom, options, back); node.append(actions);",
              "    const custom = button('カスタム学習', 'anki-button'); custom.addEventListener('click', () => void this.openStudyDialog(undefined, deck.id));\n    const exportDeck = button('デッキを書き出す (.apkg)', 'anki-button');\n    exportDeck.addEventListener('click', () => void exportAnkiPackage(deck.id).then(() => this.showStatus(`${deck.name} をAnki形式で書き出しました。`)).catch((error) => this.showStatus(error instanceof Error ? error.message : 'デッキの書き出しに失敗しました。', true)));\n    const back = button('デッキ一覧', 'anki-button'); back.addEventListener('click', () => { this.selectedDeckId = null; void this.render(); });\n    actions.append(start, custom, options, exportDeck, back); node.append(actions);")
          replace_one('src/app/app.ts',
              "noteType.replaceChildren(...state.noteTypes.map((item) => new Option(item.name, item.id)));",
              "noteType.replaceChildren(...state.noteTypes.map((item) => new Option(noteTypeDisplayName(item), item.id)));")
          replace_one('src/app/app.ts',
              "const label = el('label', { className: 'note-field-wrap', text: field.name });",
              "const label = el('label', { className: 'note-field-wrap', text: noteFieldDisplayName(field.name) });")
          replace_one('src/app/app.ts',
              "fields.append(el('p', { className: 'help', text: 'Image Occlusionはブラウザ画面の専用エディタから作成できます。' }));",
              "fields.append(el('p', { className: 'help', text: '画像穴埋めはブラウザ画面の専用エディタから作成できます。' }));")
          replace_one('src/app/app.ts',
              "const open = button('Image Occlusionエディタを開く', 'anki-button');",
              "const open = button('画像穴埋めエディタを開く', 'anki-button');")
          replace_one('src/app/app.ts',
              "if (type.kind === 'image-occlusion') throw new Error('Image Occlusionは専用エディタを使用してください。');",
              "if (type.kind === 'image-occlusion') throw new Error('画像穴埋めは専用エディタを使用してください。');")

          stats_method = r'''  private async renderStats(): Promise<void> {
    this.title.textContent = '統計';
    const [cards, history] = await Promise.all([getCards(), getHistory()]);
    const ranges = [
      { key: 'week' as const, label: '過去1週間', days: 7 },
      { key: 'month' as const, label: '過去1か月', days: 30 },
      { key: 'year' as const, label: '過去1年', days: 365 },
      { key: 'all' as const, label: '全期間', days: null }
    ];
    const selected = ranges.find((item) => item.key === this.statsRange) ?? ranges[0];
    const cutoff = selected.days == null ? null : (() => { const date = new Date(); date.setHours(0, 0, 0, 0); date.setDate(date.getDate() - selected.days + 1); return date.getTime(); })();
    const periodHistory = cutoff == null ? history : history.filter((item) => new Date(item.reviewedAt).getTime() >= cutoff);
    const correct = periodHistory.filter((item) => item.isCorrect).length;
    const totalMs = periodHistory.reduce((sum, item) => sum + Math.max(0, item.responseMs), 0);
    const periodDays = selected.days ?? (() => { if (!history.length) return 1; const first = Math.min(...history.map((item) => new Date(item.reviewedAt).getTime()).filter(Number.isFinite)); return Math.max(1, Math.floor((Date.now() - first) / 86400000) + 1); })();

    const tabs = el('div', { className: 'stats-tabs', attrs: { role: 'tablist', 'aria-label': '統計期間' } });
    for (const range of ranges) {
      const tab = button(range.label, `stats-tab${range.key === this.statsRange ? ' is-active' : ''}`);
      tab.dataset.statRange = range.key;
      tab.setAttribute('role', 'tab');
      tab.setAttribute('aria-selected', String(range.key === this.statsRange));
      tab.addEventListener('click', () => { this.statsRange = range.key; void this.render(); });
      tabs.append(tab);
    }
    this.view.append(tabs);

    const top = el('section', { className: 'settings-card stats-period-summary' });
    top.append(el('h2', { text: `期間サマリー — ${selected.label}` }));
    const metrics = el('div', { className: 'metric-grid' });
    metrics.append(
      this.metric(String(periodHistory.length), '復習数'),
      this.metric(`${periodHistory.length ? Math.round(correct / periodHistory.length * 100) : 0}%`, '正答率'),
      this.metric(formatDuration(totalMs), '学習時間'),
      this.metric((periodHistory.length / Math.max(1, periodDays)).toFixed(1), '1日平均')
    );
    top.append(metrics); this.view.append(top);

    const states = cardStateCounts(cards), ratings = ratingCounts(periodHistory), retention = trueRetention(periodHistory);
    const overview = el('section', { className: 'settings-card' }); overview.append(el('h2', { text: '現在のカード状態 / 選択期間の保持率' }));
    const stateMetrics = el('div', { className: 'metric-grid' });
    stateMetrics.append(
      this.metric(String(states.new), '新規'),
      this.metric(String(states.learning + states.relearning), '学習中'),
      this.metric(String(states.review), '復習'),
      this.metric(String(states.suspended), '保留'),
      this.metric(`${Math.round(retention.rate * 100)}%`, '真の保持率'),
      this.metric(String(streak(history)), '連続学習日数'),
      this.metric(String(ratings.again), 'もう一度'),
      this.metric(String(ratings.easy), '簡単')
    );
    overview.append(stateMetrics); this.view.append(overview);

    const chart = el('section', { className: 'settings-card' });
    const bars = el('div', { className: 'bar-list' });
    let buckets: Array<{ day: string; count: number }> = [];
    let chartTitle = '';
    if (this.statsRange === 'week' || this.statsRange === 'month') {
      const days = this.statsRange === 'week' ? 7 : 30;
      buckets = dailyCounts(history, days);
      chartTitle = this.statsRange === 'week' ? '日別推移 — 過去1週間' : '日別推移 — 過去1か月';
    } else if (this.statsRange === 'year') {
      chartTitle = '月別推移 — 過去1年';
      const now = new Date();
      for (let offset = 11; offset >= 0; offset -= 1) {
        const date = new Date(now.getFullYear(), now.getMonth() - offset, 1);
        const key = `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}`;
        buckets.push({ day: `${date.getMonth() + 1}月`, count: periodHistory.filter((item) => item.reviewedAt.startsWith(key)).length });
      }
    } else {
      chartTitle = '年別推移 — 全期間';
      const years = new Map<string, number>();
      for (const item of periodHistory) { const year = item.reviewedAt.slice(0, 4); years.set(year, (years.get(year) ?? 0) + 1); }
      buckets = [...years].sort(([a], [b]) => a.localeCompare(b)).map(([day, count]) => ({ day, count }));
      if (!buckets.length) buckets = [{ day: new Date().getFullYear().toString(), count: 0 }];
    }
    chart.append(el('h2', { text: chartTitle }));
    const max = Math.max(1, ...buckets.map((item) => item.count));
    for (const item of buckets) { const row = el('div', { className: 'bar-row' }), track = el('div', { className: 'bar-track' }), fill = el('div', { className: 'bar-fill' }); fill.style.width = `${item.count / max * 100}%`; track.append(fill); row.append(el('span', { text: item.day }), track, el('strong', { text: String(item.count) })); bars.append(row); }
    chart.append(bars); this.view.append(chart);

    const tagMap = new Map<string, { correct: number; total: number }>();
    for (const item of periodHistory) for (const tagName of item.tags) { const value = tagMap.get(tagName) ?? { correct: 0, total: 0 }; value.total += 1; if (item.isCorrect) value.correct += 1; tagMap.set(tagName, value); }
    const tagStats = [...tagMap].filter(([, value]) => value.total > 0).map(([tagName, value]) => ({ tag: tagName, accuracy: value.correct / value.total, total: value.total })).sort((a, b) => a.accuracy - b.accuracy).slice(0, 12);

    const extra = el('section', { className: 'settings-card' }); extra.append(el('h2', { text: '予測 / 間隔' }));
    const prediction = forecast(cards, 30).reduce((sum, item) => sum + item.count, 0); extra.append(el('p', { text: `今後30日の予定復習: ${prediction}枚` }));
    for (const bin of intervalHistogram(cards)) extra.append(el('p', { className: 'help', text: `${bin.label}: ${bin.count}枚` }));
    if (tagStats.length) extra.append(el('h2', { text: `タグ別正答率 — ${selected.label}` }));
    for (const item of tagStats) extra.append(el('p', { className: 'help', text: `${item.tag}: ${Math.round(item.accuracy * 100)}% (${item.total})` }));
    this.view.append(extra);
  }
'''
          replace_regex('src/app/app.ts', r"  private async renderStats\(\): Promise<void> \{.*?\n  \}\n\n  private async renderSettings", stats_method + "\n  private async renderSettings")

          replace_one('src/app/app.ts',
              "    const imp = button('読み込み', 'anki-button'), json = button('完全JSON', 'anki-button'), csv = button('CSV', 'anki-button'); imp.addEventListener('click', () => this.importFile.click()); json.addEventListener('click', () => void exportJson()); csv.addEventListener('click', () => void exportCsv()); row.append(imp, json, csv); data.append(row); grid.append(data); this.view.append(grid);",
              "    const imp = button('Ankiデッキを読み込む', 'anki-button'), anki = button('Anki形式で書き出す', 'anki-button'), json = button('完全JSON', 'anki-button'), csv = button('CSV', 'anki-button'); imp.addEventListener('click', () => this.importFile.click()); anki.addEventListener('click', () => void exportAnkiPackage().then(() => this.showStatus('コレクションをAnki形式で書き出しました。')).catch((error) => this.showStatus(error instanceof Error ? error.message : '書き出しに失敗しました。', true))); json.addEventListener('click', () => void exportJson()); csv.addEventListener('click', () => void exportCsv()); row.append(imp, anki, json, csv); data.append(row); grid.append(data); this.view.append(grid);")
          replace_one('src/app/app.ts',
              "      } else if (name.endsWith('.apkg') || name.endsWith('.colpkg')) this.showStatus('AnkiパッケージのネイティブSQLite/Mediaコンテナ互換は次の互換レイヤーで処理します。', true, 7000);",
              "      } else if (name.endsWith('.apkg') || name.endsWith('.colpkg')) { const result = await importAnkiPackage(file); this.showStatus(`Ankiデッキを読み込みました：デッキ${result.decks}・ノート${result.notes}・カード${result.cards}・履歴${result.history}・メディア${result.media}`); }")

          # center.ts localization and per-deck exports
          replace_one('src/anki/center.ts',
              "import { DEFAULT_DECK_ID, IMAGE_OCCLUSION_NOTE_TYPE_ID } from './defaults.js';",
              "import { DEFAULT_DECK_ID, IMAGE_OCCLUSION_NOTE_TYPE_ID } from './defaults.js';\nimport { noteFieldDisplayName, noteTypeDisplayName, noteTypeKindDisplayName } from './localization.js';\nimport { exportAnkiPackage } from './anki-package.js';")
          replace_one('src/anki/center.ts',
              "function selectFrom<T extends { id: string; name: string }>(items: T[], value?: string): HTMLSelectElement {\n  const select = el('select');\n  for (const item of items) select.append(new Option(item.name, item.id, false, item.id === value));\n  return select;\n}",
              "function selectFrom<T extends { id: string; name: string }>(items: T[], value?: string): HTMLSelectElement {\n  const select = el('select');\n  for (const item of items) select.append(new Option(item.name, item.id, false, item.id === value));\n  return select;\n}\n\nfunction selectNoteTypes(items: NoteTypeDefinition[], value?: string): HTMLSelectElement {\n  const select = el('select');\n  for (const item of items) select.append(new Option(noteTypeDisplayName(item), item.id, false, item.id === value));\n  return select;\n}")
          replace_one('src/anki/center.ts', "const clozeButton=button('Cloze','small-button');", "const clozeButton=button('穴埋め','small-button');")
          replace_one('src/anki/center.ts',
              "const node = section('Anki互換センター', 'デッキ、ノートタイプ、テンプレート、Cloze、画像穴埋め、検索、フィルターデッキ、FSRS設定、バックアップ等を管理します。');",
              "const node = section('Anki互換センター', 'デッキ、ノートタイプ、テンプレート、穴埋め、画像穴埋め、検索、フィルターデッキ、FSRS設定、バックアップ等を管理します。');")
          replace_one('src/anki/center.ts',
              "      const rename = button('名前変更', 'small-button');",
              "      const exportDeck = button('書き出し', 'small-button');\n      exportDeck.addEventListener('click', () => void exportAnkiPackage(deck.id).then(() => this.callbacks.showStatus(`${deck.name} をAnki形式で書き出しました。`)).catch((error) => this.callbacks.showStatus(error instanceof Error ? error.message : '書き出しに失敗しました。', true)));\n      const rename = button('名前変更', 'small-button');")
          replace_one('src/anki/center.ts', "actions.prepend(study, child, rename);", "actions.prepend(study, child, rename, exportDeck);")
          replace_one('src/anki/center.ts',
              "const node = section('ノートを追加', 'Basic / Reverse / Type Answer / Cloze / Image Occlusion / カスタムノートタイプに対応します。');",
              "const node = section('ノートを追加', '基本 / 表裏カード / 解答入力 / 穴埋め / 画像穴埋め / カスタムノートタイプに対応します。');")
          replace_one('src/anki/center.ts', "const typeSelect = selectFrom(state.noteTypes);", "const typeSelect = selectNoteTypes(state.noteTypes);")
          replace_one('src/anki/center.ts', "const control = labeledTextarea(field.name, '', field.name.toLowerCase().includes('text') ? 6 : 3);", "const control = labeledTextarea(noteFieldDisplayName(field.name), '', field.name.toLowerCase().includes('text') ? 6 : 3);")
          replace_one('src/anki/center.ts', "const extra = labeledTextarea('Extra', '', 3);", "const extra = labeledTextarea('補足', '', 3);")
          replace_one('src/anki/center.ts', "const base = selectFrom(state.noteTypes);", "const base = selectNoteTypes(state.noteTypes);")
          replace_one('src/anki/center.ts',
              "const info = el('div'); info.append(el('strong', { text: type.name }), el('p', { className: 'help', text: `${type.kind} · ${type.fields.map((field) => field.name).join(', ')} · ${type.templates.length} template(s)` }));",
              "const info = el('div'); info.append(el('strong', { text: noteTypeDisplayName(type) }), el('p', { className: 'help', text: `${noteTypeKindDisplayName(type.kind)} · ${type.fields.map((field) => noteFieldDisplayName(field.name)).join(', ')} · テンプレート${type.templates.length}件` }));")
          replace_one('src/anki/center.ts',
              "    const actions = el('div', { className: 'button-row' });\n    const exportButton = button('コレクションを書き出す');",
              "    const actions = el('div', { className: 'button-row' });\n    const ankiImport = button('Ankiデッキを読み込む (.apkg/.colpkg)'); ankiImport.addEventListener('click', () => (document.getElementById('import-file') as HTMLInputElement | null)?.click());\n    const ankiExport = button('コレクションをAnki形式で書き出す (.apkg)'); ankiExport.addEventListener('click', () => void exportAnkiPackage().then(() => this.callbacks.showStatus('コレクションをAnki形式で書き出しました。')).catch((error) => this.callbacks.showStatus(error instanceof Error ? error.message : '書き出しに失敗しました。', true)));\n    const exportButton = button('コレクションを書き出す');")
          replace_one('src/anki/center.ts',
              "actions.append(exportButton, importButton, importFile, textButton, textFile, backup); node.append(actions);",
              "actions.append(ankiImport, ankiExport, exportButton, importButton, importFile, textButton, textFile, backup); node.append(actions);")

          # anki-package.ts: deck-scoped export while retaining full collection export when deckId is omitted.
          package = Path('src/anki/anki-package.ts')
          text = package.read_text()
          old_start = "export async function exportAnkiPackage():Promise<void>{\n  const[state,allCards,history]=await Promise.all([getAnkiState(),getCards(false,true),getHistory()]),profileId=state.activeProfileId,notes=state.notes.filter((note)=>note.profileId===profileId&&!note.deletedAt),noteIds=new Set(notes.map((note)=>note.id)),cards=allCards.filter((card)=>(card.profileId??profileId)===profileId&&!card.deletedAt&&noteIds.has(card.noteId??'')),noteTypes=state.noteTypes.filter((type)=>notes.some((note)=>note.noteTypeId===type.id)),decks=state.decks.filter((deck)=>deck.profileId===profileId),SQL=await sqlRuntime(),db=new SQL.Database(),nowSeconds=Math.floor(Date.now()/1000),creationSeconds=nowSeconds-(nowSeconds%86400);"
          new_start = "export async function exportAnkiPackage(deckId?:string):Promise<void>{\n  const[state,allCards,history]=await Promise.all([getAnkiState(),getCards(false,true),getHistory()]);\n  const profileId=state.activeProfileId,profileDecks=state.decks.filter((deck)=>deck.profileId===profileId),selectedDeck=deckId?profileDecks.find((deck)=>deck.id===deckId):undefined;\n  if(deckId&&!selectedDeck)throw new Error('デッキが見つかりません。');\n  const scopedDecks=selectedDeck?profileDecks.filter((deck)=>deck.id===selectedDeck.id||deck.name.startsWith(`${selectedDeck.name}::`)):profileDecks;\n  const cardDeckIds=new Set(scopedDecks.map((deck)=>deck.id));\n  const cards=allCards.filter((card)=>(card.profileId??profileId)===profileId&&!card.deletedAt&&cardDeckIds.has(card.deckId??''));\n  const noteIds=new Set(cards.map((card)=>card.noteId).filter((id):id is string=>Boolean(id)));\n  const notes=state.notes.filter((note)=>note.profileId===profileId&&!note.deletedAt&&noteIds.has(note.id));\n  const noteTypes=state.noteTypes.filter((type)=>notes.some((note)=>note.noteTypeId===type.id));\n  const exportDeckIds=new Set(cardDeckIds);\n  if(selectedDeck){for(const deck of scopedDecks){const parts=deck.name.split('::');for(let depth=1;depth<parts.length;depth+=1){const parentName=parts.slice(0,depth).join('::'),parent=profileDecks.find((item)=>item.name===parentName);if(parent)exportDeckIds.add(parent.id);}}}\n  const decks=profileDecks.filter((deck)=>exportDeckIds.has(deck.id));\n  const SQL=await sqlRuntime(),db=new SQL.Database(),nowSeconds=Math.floor(Date.now()/1000),creationSeconds=nowSeconds-(nowSeconds%86400);"
          if old_start not in text:
              raise SystemExit('exportAnkiPackage start not found')
          text = text.replace(old_start, new_start, 1)
          old_download = "anchor.download=`collection-${new Date().toISOString().slice(0,10)}.apkg`;"
          new_download = "anchor.download=selectedDeck?`${sanitizeFilename(selectedDeck.name.replace(/::/g,' - '))}.apkg`:`collection-${new Date().toISOString().slice(0,10)}.apkg`;"
          if old_download not in text:
              raise SystemExit('Anki download filename target not found')
          package.write_text(text.replace(old_download, new_download, 1))

          # index and exact UI Japanese labels
          replace_one('index.html', '<button type="button" data-editor-command="cloze">[…] Cloze</button>', '<button type="button" data-editor-command="cloze">[…] 穴埋め</button>')
          replace_one('src/exact-ui.ts', "menuButton('読み込み…', 'Ctrl+Shift+I'", "menuButton('Ankiデッキを読み込む…', 'Ctrl+Shift+I'")
          replace_one('src/exact-ui.ts', "['1か月', '3か月', '1年', '全期間']", "['過去1週間', '過去1か月', '過去1年', '全期間']")

          # Integration acceptance for localization, period statistics, deck-scoped APKG export and re-import.
          Path('tests/deck-portability-stats.e2e.mjs').write_text(r'''import assert from 'node:assert/strict';
import http from 'node:http';
import { mkdtemp, readFile, stat } from 'node:fs/promises';
import os from 'node:os';
import path from 'node:path';
import { chromium, webkit } from 'playwright';
import { unzipSync } from 'fflate';
import initSqlJs from 'sql.js';

const dist = path.resolve('dist');
const mime = { '.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json','.svg':'image/svg+xml','.wasm':'application/wasm','.webmanifest':'application/manifest+json' };
const server = http.createServer(async (req,res)=>{try{const pathname=new URL(req.url??'/','http://localhost').pathname;let file=path.join(dist,pathname==='/'?'index.html':pathname.replace(/^\//,''));if((await stat(file)).isDirectory())file=path.join(file,'index.html');res.writeHead(200,{'Content-Type':mime[path.extname(file)]??'application/octet-stream','Cache-Control':'no-store'});res.end(await readFile(file));}catch{res.writeHead(404);res.end('not found');}});
await new Promise((resolve)=>server.listen(0,'127.0.0.1',resolve));
const address=server.address();if(!address||typeof address==='string')throw new Error('server');const url=`http://127.0.0.1:${address.port}/`;
const temp=await mkdtemp(path.join(os.tmpdir(),'work1-deck-'));
const SQL=await initSqlJs();

async function addHistory(page, cardId) {
  await page.evaluate(async ({ cardId }) => {
    const db = await new Promise((resolve,reject)=>{const r=indexedDB.open('work_1_study_cards');r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});
    const tx=db.transaction('history','readwrite'); const store=tx.objectStore('history');
    const days=[5,20,200,500];
    days.forEach((daysAgo,index)=>{const d=new Date();d.setDate(d.getDate()-daysAgo);const iso=d.toISOString();store.put({id:`stats_${index}`,cardId,questionSnapshot:'Q',tags:['統計タグ'],rating:index===1?'again':'good',isCorrect:index!==1,responseMs:60000,reviewedAt:iso,nextDue:iso,device:'e2e',requestId:`stats_req_${index}`});});
    await new Promise((resolve,reject)=>{tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error);});
  }, { cardId });
}

async function periodCount(page, label) {
  await page.getByRole('tab', { name: label }).click();
  const summary=page.locator('.stats-period-summary');
  return Number(await summary.locator('.metric').filter({hasText:'復習数'}).locator('strong').innerText());
}

async function run(name, engine) {
  const browser=await engine.launch({headless:true});
  try {
    const context=await browser.newContext({viewport:{width:1024,height:768},acceptDownloads:true,serviceWorkers:'allow'});
    const page=await context.newPage(); await page.goto(url,{waitUntil:'networkidle'}); await page.waitForSelector('.main-toolbar');

    await page.getByRole('button',{name:'追加',exact:true}).click();
    const optionLabels=await page.locator('#note-type option').allTextContents();
    for(const expected of ['基本','基本（表裏2枚）','基本（任意で表裏2枚）','基本（解答入力）','穴埋め','画像穴埋め']) assert.ok(optionLabels.includes(expected),`${name}: Japanese note type ${expected}`);
    assert.ok(await page.getByLabel('表面').isVisible(),`${name}: Front field localized`);
    assert.ok(await page.getByLabel('裏面').isVisible(),`${name}: Back field localized`);
    await page.getByRole('button',{name:'閉じる',exact:true}).click();

    page.once('dialog',(dialog)=>dialog.accept('書き出しテスト'));
    await page.getByRole('button',{name:'デッキを作成',exact:true}).click();
    await page.waitForFunction(()=>document.getElementById('view')?.textContent?.includes('書き出しテスト'));

    await page.getByRole('button',{name:'追加',exact:true}).click();
    await page.locator('#note-deck').selectOption({label:'書き出しテスト'});
    await page.getByLabel('表面').fill('書き出し問題');
    await page.getByLabel('裏面').fill('書き出し答え');
    await page.locator('#card-save').click();
    await page.waitForFunction(()=>document.getElementById('status-message')?.textContent?.includes('カードを追加'));
    await page.getByRole('button',{name:'閉じる',exact:true}).click();

    const cardId=await page.evaluate(async()=>{const db=await new Promise((resolve,reject)=>{const r=indexedDB.open('work_1_study_cards');r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});const tx=db.transaction('cards','readonly');const cards=await new Promise((resolve,reject)=>{const r=tx.objectStore('cards').getAll();r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});return cards.find((card)=>card.question.includes('書き出し問題'))?.id;});
    assert.ok(cardId,`${name}: card created`); await addHistory(page,cardId);

    await page.locator('[data-route="home"]').click();
    await page.getByRole('button',{name:'書き出しテスト',exact:true}).click();
    const downloadPromise=page.waitForEvent('download');
    await page.getByRole('button',{name:'デッキを書き出す (.apkg)',exact:true}).click();
    const download=await downloadPromise; const saved=path.join(temp,`${name}-書き出しテスト.apkg`); await download.saveAs(saved);
    assert.match(download.suggestedFilename(),/書き出しテスト\.apkg$/,`${name}: deck-specific filename`);
    const archive=unzipSync(new Uint8Array(await readFile(saved))); assert.ok(archive['collection.anki21'],`${name}: legacy-compatible Anki collection present`);
    const db=new SQL.Database(archive['collection.anki21']);
    assert.equal(db.exec('select count(*) from notes')[0].values[0][0],1,`${name}: one exported note`);
    assert.equal(db.exec('select count(*) from cards')[0].values[0][0],1,`${name}: one exported card`);
    const decks=JSON.parse(String(db.exec('select decks from col')[0].values[0][0]));
    assert.ok(Object.values(decks).some((deck)=>deck.name==='書き出しテスト'),`${name}: selected deck exported`); db.close();

    await page.locator('[data-route="stats"]').click();
    assert.equal(await periodCount(page,'過去1週間'),1,`${name}: week stats`);
    assert.equal(await periodCount(page,'過去1か月'),2,`${name}: month stats`);
    assert.equal(await periodCount(page,'過去1年'),3,`${name}: year stats`);
    assert.equal(await periodCount(page,'全期間'),4,`${name}: all-time stats`);

    const clean=await browser.newContext({viewport:{width:1024,height:768},serviceWorkers:'allow'}); const imported=await clean.newPage();
    await imported.goto(url,{waitUntil:'networkidle'}); await imported.locator('#import-file').setInputFiles(saved);
    await imported.waitForFunction(()=>document.getElementById('status-message')?.textContent?.includes('Ankiパッケージを読み込みました'));
    await imported.waitForTimeout(900); await imported.waitForSelector('.main-toolbar');
    assert.ok((await imported.locator('#view').innerText()).includes('書き出しテスト'),`${name}: exported Anki deck can be re-imported`);
    await clean.close(); await context.close(); process.stdout.write(`deck-portability-stats:${name}:pass\n`);
  } finally { await browser.close(); }
}
for(const [name,engine] of [['chromium',chromium],['webkit',webkit]]) await run(name,engine);
server.close(); process.stdout.write('deck-portability-stats-e2e: chromium + webkit ok\n');
''')

          replace_one('package.json',
              '"e2e": "npm run build && node tests/browser.e2e.mjs && node tests/full-function.e2e.mjs && node tests/persistence.e2e.mjs"',
              '"e2e": "npm run build && node tests/browser.e2e.mjs && node tests/full-function.e2e.mjs && node tests/persistence.e2e.mjs && node tests/deck-portability-stats.e2e.mjs"')

          audit=Path('ANKI_AUDIT_2026-08-15.md')
          audit_text=audit.read_text()
          audit_text=audit_text.replace('- Current `.colpkg` / `.apkg` import paths plus legacy-compatible `.apkg` export.', '- Current `.colpkg` / `.apkg` import paths plus legacy-compatible full-collection and per-deck `.apkg` export.')
          audit_text=audit_text.replace('- IndexedDB persistence, offline PWA cache and same-device session/history persistence.', '- Japanese-localized stock note-type UI, IndexedDB persistence, offline PWA cache and same-device session/history persistence.')
          audit.write_text(audit_text)
      - name: Validate before commit
        run: |
          npm install --ignore-scripts --no-audit
          npm run lint
          npm run typecheck
          npm test
          npm run security
      - name: Commit implementation
        run: |
          git config user.name "work_1 review bot"
          git config user.email "actions@users.noreply.github.com"
          git add src index.html tests package.json ANKI_AUDIT_2026-08-15.md
          git commit -m "localize note UI and add portable deck workflows"
          git push origin HEAD:agent/japanese-deck-import-export-stats

export function el<K extends keyof HTMLElementTagNameMap>(tag:K,options:{className?:string;text?:string;attrs?:Record<string,string>}={},children:Node[]=[]):HTMLElementTagNameMap[K]{const node=document.createElement(tag);if(options.className)node.className=options.className;if(options.text!==undefined)node.textContent=options.text;if(options.attrs)for(const[k,v]of Object.entries(options.attrs))node.setAttribute(k,v);if(children.length)node.append(...children);return node;}
export function clear(node:Element):void{node.replaceChildren();}
export function button(text:string,className='secondary-button'):HTMLButtonElement{return el('button',{className,text,attrs:{type:'button'}});}

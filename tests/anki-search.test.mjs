import test from 'node:test';import assert from 'node:assert/strict';
import { createDefaultAnkiState, DEFAULT_PROFILE_ID, DEFAULT_DECK_ID, BASIC_NOTE_TYPE_ID } from '../dist/assets/anki/defaults.js';
import { emptyCard } from '../dist/assets/anki/defaults.js';import { searchCards, tokenizeSearch } from '../dist/assets/anki/search.js';
const state=createDefaultAnkiState();const note={id:'note1',guid:'g',profileId:DEFAULT_PROFILE_ID,deckId:DEFAULT_DECK_ID,noteTypeId:BASIC_NOTE_TYPE_ID,fields:{Front:'Café capital',Back:'Paris',Extra:''},tags:['geo::europe'],createdAt:'2026-01-01T00:00:00.000Z',updatedAt:'2026-01-01T00:00:00.000Z',deletedAt:null};state.notes=[note];const card={...emptyCard(new Date('2026-01-01T00:00:00Z')),id:'card1',noteId:'note1',deckId:DEFAULT_DECK_ID,noteTypeId:BASIC_NOTE_TYPE_ID,templateId:'template_basic_forward',question:'Café capital',answer:'Paris',tags:['geo::europe'],flag:3,marked:true,suspended:false,queue:'review',schedule:{...emptyCard().schedule,reps:4,lapses:1,stability:12,difficulty:4,due:'2026-01-01T00:00:00Z',lastReview:'2025-12-20T00:00:00Z'}};
test('tokenizer keeps quoted phrases',()=>assert.deepEqual(tokenizeSearch('deck:"Default" tag:geo'),['deck:Default','tag:geo']));
test('tag hierarchy and deck search',()=>{assert.equal(searchCards([card],state,[],'tag:geo').length,1);assert.equal(searchCards([card],state,[],'deck:Default').length,1)});
test('field, flag, state and property searches',()=>{assert.equal(searchCards([card],state,[],'Front:"café capital"').length,1);assert.equal(searchCards([card],state,[],'flag:3 is:marked prop:reps>=4').length,1);assert.equal(searchCards([card],state,[],'-is:suspended').length,1)});
test('regex and accent-insensitive plain search',()=>{assert.equal(searchCards([card],state,[],'cafe').length,1);assert.equal(searchCards([card],state,[],'/Paris/i').length,0);assert.equal(searchCards([card],state,[],'/Paris/').length,1)});
